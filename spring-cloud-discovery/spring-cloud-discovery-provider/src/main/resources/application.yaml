server:
  port: 8081

spring:
  application:
    name: nacos-provider

  cloud:
    nacos:
      username: nacos
      password: nacos
      server-addr: localhost:8848
      discovery:
        server-addr: localhost:8848